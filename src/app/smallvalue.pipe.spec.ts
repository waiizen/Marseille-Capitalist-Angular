import { SmallvaluePipe } from './smallvalue.pipe';

describe('SmallvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new SmallvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
